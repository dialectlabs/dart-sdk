name: dialect_sdk
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.2 <3.0.0'


dependencies: 
  borsh: ^0.3.0+3
  borsh_annotation: ^0.3.1+1
  build_runner: ^2.1.11
  crypto: ^3.0.2
  cryptography: ^2.0.5
  hex: ^0.2.0
  http: ^0.13.4
  json_serializable: ^6.2.0
  pinenacl: ^0.3.4
  recase: ^4.0.0
  shared_preferences: ^2.0.15
  solana: ^0.25.0
  solana_name_service:
    path: ../dart-solana-name-service
  cardinal_namespaces:
    path: ../dart-cardinal-twitter
#   path: ^1.8.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
